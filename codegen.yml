overwrite: true
schema: "https://one-airedale-51.hasura.app/v1/graphql"
documents: "queries/**/*.ts"
generates:
  types/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
